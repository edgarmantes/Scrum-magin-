import { renderComponent, expect } from '../test_helper';
import Signin from '../../js/components/signin';
import ReactTestUtils from 'react-dom/test-utils';

describe('Sign In', () => {

	let component;
	
	// beforeEach( () => {
		component = renderComponent(Signin);
	// });

	it('has a class name of singin-container', () => {
		// expect(component).to.have.class('signin-container');
	})

	// it('has a fieldset tag', () => {

	// })

	// it('has a username input', () => {})
	// it('has a password input', () => {})
	// it('has a submit button', () => {})
	// it('has a demo link', () => {})

})